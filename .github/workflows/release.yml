name: Release packages

on:
  workflow_dispatch:
    inputs:
      publish_npm:
        description: Publish gloss-local to npm (requires NPM_TOKEN)
        required: false
        default: false
        type: boolean
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  npm-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Build npm tarball
        run: npm pack
      - uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: gloss-local-*.tgz

  source-archive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create source archive
        shell: bash
        run: |
          mkdir release-assets
          git archive --format=tar.gz --prefix="Gloss-${GITHUB_REF_NAME#v}/" \
            --output="release-assets/Gloss-source-${GITHUB_REF_NAME#v}.tar.gz" HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: Gloss-source
          path: release-assets/*

  desktop-package:
    name: ${{ matrix.os }} ${{ matrix.arch }} desktop package
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows
            arch: x64
            runner: windows-latest
            artifact_name: Gloss-windows-x64
            asset_name: Gloss-windows-x64.zip
          - os: windows
            arch: arm64
            runner: windows-11-arm
            artifact_name: Gloss-windows-arm64
            asset_name: Gloss-windows-arm64.zip
          - os: macos
            arch: x64
            runner: macos-15-intel
            artifact_name: Gloss-macos-x64
            asset_name: Gloss-macos-x64.tar.gz
          - os: macos
            arch: arm64
            runner: macos-14
            artifact_name: Gloss-macos-arm64
            asset_name: Gloss-macos-arm64.tar.gz
          - os: linux
            arch: x64
            runner: ubuntu-24.04
            artifact_name: Gloss-linux-x64
            asset_name: Gloss-linux-x64.tar.gz
          - os: linux
            arch: arm64
            runner: ubuntu-24.04-arm
            artifact_name: Gloss-linux-arm64
            asset_name: Gloss-linux-arm64.tar.gz
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            packaging/requirements-build.txt
      - name: Build portable Windows executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./scripts/build-windows.ps1
      - name: Build portable macOS or Linux executable
        if: runner.os != 'Windows'
        shell: bash
        run: |
          npm run build:frontend
          python -m pip install --upgrade pip
          python -m pip install -r backend/requirements.txt -r packaging/requirements-build.txt
          python -m PyInstaller --noconfirm --clean packaging/Gloss.spec
      - name: Smoke test Windows executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: ./dist/Gloss.exe --version
      - name: Smoke test macOS or Linux executable
        if: runner.os != 'Windows'
        shell: bash
        run: ./dist/Gloss --version
      - name: Create Windows release assets
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force release-assets | Out-Null
          Compress-Archive -Path dist/Gloss.exe -DestinationPath "release-assets/${{ matrix.asset_name }}"
          if ('${{ matrix.arch }}' -eq 'x64') {
            # Keep the original direct-download asset used by the README.
            Copy-Item dist/Gloss.exe release-assets/Gloss.exe
          }
      - name: Create macOS or Linux release archive
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p release-assets/package
          cp dist/Gloss release-assets/package/Gloss
          chmod +x release-assets/package/Gloss
          tar -C release-assets/package -czf "release-assets/${{ matrix.asset_name }}" Gloss
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: release-assets/*

  github-release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [npm-package, source-archive, desktop-package]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: "*"
          merge-multiple: true
          path: release-assets
      - uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true

  publish-npm:
    if: github.event_name == 'workflow_dispatch' && inputs.publish_npm
    needs: npm-package
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: npm-package
      - uses: actions/setup-node@v4
        with:
          # npm trusted publishing requires Node 22.14+ and npm 11.5.1+.
          node-version: "24"
          registry-url: https://registry.npmjs.org
      - name: Publish npm package
        run: npm publish gloss-local-*.tgz --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
